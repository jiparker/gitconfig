[core]
	editor = nano
	mergeoptions = --no-edit

[color]
	ui = true

[user]
	name = cory.huebner
	email = cory.huebner@asolutions.com

[credential]
	helper = !'/home/michael.petter/AppData/Roaming/GitCredStore/git-credential-winstore.exe'
#	helper = cache --timeout=86400

[push]
	default = upstream

[difftool "codecompare"]
	cmd = '/c/Program Files/Devart/Code Compare/CodeCompare.exe' \"$(cygpath -w \"$LOCAL\")\" \"$(cygpath -w \"$REMOTE\")\"
	renames = true
 
[diff]
	tool = codecompare
 
[mergetool "codecompare"]
	cmd = '/c/Program Files/Devart/Code Compare/CodeMerge.exe' -MF=\"$(cygpath -w \"$LOCAL\")\" -TF=\"$(cygpath -w \"$REMOTE\")\" -BF=\"$(cygpath -w \"$BASE\")\" -RF=\"$(cygpath -w \"$MERGED\")\"
	trustExitCode = true
 
[mergetool]
	keepBackup = false
 
[merge]
	tool = codecompare

[difftool]
	prompt = false
	
[alias]
	co = checkout
	ci = commit
	st = status
	br = branch
	rhl = reset --hard head
	rhu = !git reset --hard $(git rev-parse --symbolic-full-name @{u})
	put = push origin HEAD
	dt = difftool
	deletebr = !sh -c 'git push origin :$1 && git branch -D $1' -

[http]
	sslVerify = false
